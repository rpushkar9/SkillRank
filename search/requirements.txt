# Core ML dependencies - install PyTorch via conda first (see README)
# torch>=2.4.0  # Install via conda: conda install pytorch -c pytorch
numpy<2.0.0  # NumPy 2.x incompatible with some compiled modules
rank-bm25
sentence-transformers>=2.3.0
scikit-learn
pandas
python-dateutil
nltk

# Web UI
flask>=3.0.0
